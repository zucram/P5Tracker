import { readFile, writeFile, mkdir } from 'node:fs/promises';
import { fileURLToPath } from 'node:url';
import path from 'node:path';
import { lookupForm } from './guide-helpers.mjs';
import { availablePersonas, fusePersonas, SOURCE_URL } from '../src/p3/fusion.js';

const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..');
const load = async name => JSON.parse(await readFile(path.join(root, 'knowledge/p3-reload', `${name}.json`), 'utf8'));
const [dialogue, requests, tartarus, provenance] = await Promise.all(['social-link-dialogue', 'requests', 'tartarus', 'reference/provenance'].map(load));
const reviewed = '2026-09-07';
const site = 'https://zucram.github.io/P5Tracker/';
const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[c]);
const date = value => /^\d{2}-\d{2}$/.test(value || '') ? `${['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'][Number(value.slice(0, 2)) - 1]} ${Number(value.slice(3))}` : 'Not listed';
const slugify = value => value.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
const list = (items, ordered = false) => items?.length ? `<${ordered ? 'ol' : 'ul'}>${items.map(text => `<li>${esc(text)}</li>`).join('')}</${ordered ? 'ol' : 'ul'}>` : '';
const paragraphs = value => (Array.isArray(value) ? value : [value]).filter(Boolean).map(text => `<p>${esc(text)}</p>`).join('');
function sourceLink(source) {
  const url = new URL(source.url);
  if (!['http:', 'https:'].includes(url.protocol)) throw new Error('Invalid source protocol');
  return `<a href="${esc(source.url)}">${esc(source.label || url.hostname.replace(/^www\./, ''))}</a>${source.locator ? `: ${esc(source.locator)}` : ''}`;
}
const sources = entries => entries?.length ? `<p class="sources">Sources: ${entries.map(sourceLink).join(' · ')}</p>` : '';
const allGuides = [
  ['persona-3-reload-school-answers', 'School and exam answers'],
  ['persona-3-reload-social-links', 'Social Link requirements and schedules'],
  ['persona-3-reload-deadlines', 'Rescues and Linked Episode deadlines'],
  ['persona-3-reload-social-link-answers', 'Social Link rank answers'],
  ['persona-3-reload-elizabeth-requests', "Elizabeth's 101 requests"],
  ['persona-3-reload-fusion-guide', 'Fusion recipes and Persona unlocks'],
];
const tracker = (slug, tab, label) => `<a class="cta" href="../../p3/#${tab}" data-umami-event="guide_open_tracker" data-umami-event-guide="${slug}">${esc(label)}</a>`;
function page({ slug, title, description, intro, body, tab, cta }) {
  const canonical = `${site}guides/${slug}/`;
  const schema = { '@context': 'https://schema.org', '@graph': [
    { '@type': 'WebPage', '@id': canonical, name: title, description, url: canonical, inLanguage: 'en', dateModified: reviewed, about: { '@type': 'VideoGame', name: 'Persona 3 Reload' }, breadcrumb: { '@id': `${canonical}#breadcrumbs` } },
    { '@type': 'BreadcrumbList', '@id': `${canonical}#breadcrumbs`, itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'All games', item: site },
      { '@type': 'ListItem', position: 2, name: 'Persona 3 Reload', item: `${site}p3/` },
      { '@type': 'ListItem', position: 3, name: title, item: canonical },
    ] },
  ] };
  return `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<title>${esc(title)} | P5 Tracker</title><meta name="description" content="${esc(description)}"><meta name="robots" content="index,follow"><link rel="canonical" href="${canonical}"><link rel="icon" href="../../favicon.svg"><link rel="stylesheet" href="../guide.css"><link rel="stylesheet" href="../p3-guide.css"><link rel="stylesheet" href="../lookup.css"><script type="module" src="../lookup-ui.js"></script>
<meta property="og:type" content="website"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}"><meta property="og:url" content="${canonical}"><meta property="og:site_name" content="P5 Tracker"><meta name="twitter:card" content="summary"><meta name="twitter:title" content="${esc(title)}"><meta name="twitter:description" content="${esc(description)}">
<script type="application/ld+json">${JSON.stringify(schema).replace(/</g, '\\u003c')}</script>
<script defer src="https://cloud.umami.is/script.js" data-website-id="7fae2edd-7137-49ac-8ec6-714a18a48e3f" data-domains="zucram.github.io"></script>
<style>.playthrough-guide main{max-width:980px}.playthrough-guide main>*{min-width:0}.playthrough-guide code{overflow-wrap:anywhere}.playthrough-guide details{border:1px solid #425165;border-radius:10px;background:#14202c;padding:12px 18px;margin:12px 0;overflow-wrap:anywhere}.playthrough-guide summary{cursor:pointer;font-weight:700;min-height:32px}.playthrough-guide details:target{outline:2px solid #83d9ff;outline-offset:3px}.playthrough-guide details details{background:#111b25}.playthrough-guide .sources{font-size:.8rem;line-height:1.7;color:#b6c5d5;overflow-wrap:anywhere}.playthrough-guide .jump-list{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:8px 20px;padding-left:24px}.playthrough-guide .jump-list li{font-size:.9rem;line-height:1.6}.playthrough-guide .rank-links{font-size:.9rem}.playthrough-guide .rank-links a{display:inline-block;margin:0 10px 8px 0}.playthrough-guide .choice{border-left:2px solid #47788d;padding:4px 12px;margin:8px 0}.playthrough-guide .points,.playthrough-guide .route{display:block;font-size:.85rem}.playthrough-guide .route{color:#efcca2}.playthrough-guide .points{color:#9de0c9}.playthrough-guide h3{font-size:1.15rem;line-height:1.4}.playthrough-guide .back-index{font-size:.8rem}.playthrough-guide section{scroll-margin-top:12px}.playthrough-guide .recipe-list{list-style:none;padding:0}.playthrough-guide .recipe-list li{padding:10px 0;border-bottom:1px solid #425165}.playthrough-guide .breadcrumb{font-size:.85rem;color:#b6c5d5}.playthrough-guide .meta{font-size:.9rem;color:#b6c5d5}@media(max-width:600px){.playthrough-guide main{padding:24px 16px}.playthrough-guide details{padding:12px}.playthrough-guide .jump-list{grid-template-columns:1fr}.playthrough-guide h1{font-size:2rem}.playthrough-guide .cta{box-sizing:border-box;max-width:100%;font-size:1rem}}</style></head>
<body class="reload-guide playthrough-guide"><a class="skip" href="#content">Skip to content</a><main id="content"><nav aria-label="Main"><a href="../../">All games</a> · <a href="../../p3/">P3 Reload tracker</a> · <a href="../">All guides</a></nav><p class="breadcrumb"><a href="../../">All games</a> / <a href="../../p3/">Persona 3 Reload</a> / ${esc(title)}</p>
<h1>${esc(title)}</h1><p>${esc(intro)}</p><p class="meta">Main campaign · Last reviewed <time datetime="${reviewed}">${reviewed}</time></p><p>${tracker(slug, tab, cta)}</p>
${body}
<section><h2>Coverage and sources</h2><p>The Reload tracker is in beta. This reference covers the listed main-campaign mechanics and excludes Episode Aigis. It does not promise a perfect run or a fixed daily route. Source links accompany the entries. Character identities, late-game unlocks and story choices can contain spoilers.</p><p>All entries are present in this page's HTML. Opening a disclosure works without JavaScript. Saving progress and calculating other fusion recipes use the interactive tracker.</p></section>
<nav class="related" aria-label="Related Reload guides">${allGuides.filter(([other]) => other !== slug).map(([other, label]) => `<a href="../${other}/">${esc(label)}</a>`).join(' · ')}</nav><p>${tracker(slug, tab, cta)}</p><footer><p>Unofficial fan tool. Not affiliated with ATLUS or SEGA. Umami measures visits and link clicks. This guide does not collect game saves.</p><p><a href="https://ko-fi.com/K3K11RWTSL" data-umami-event="guide_support_click" data-umami-event-guide="${slug}">Support content checks and updates on Ko-fi</a></p></footer></main></body></html>\n`;
}

const socialSlug = 'persona-3-reload-social-link-answers';
const socialBody = `<p class="notice">These are short response cues, not dialogue transcripts. Character names are visible in the index. Rank choices and romance details stay inside disclosures until opened.</p>${lookupForm({ guide: socialSlug, label: 'Find a Social Link by character or Arcana', placeholder: 'For example, Yukari or Hermit', noun: 'Social Links' })}<section id="social-index" data-lookup-intro><h2>Answers by character and Arcana</h2><ul class="jump-list">${dialogue.links.map(link => `<li><a href="#${esc(link.id)}">${esc(link.name)} · ${esc(link.arcana)}</a></li>`).join('')}</ul></section><section data-lookup-intro><h2>Affinity points, notes and relationship branches</h2>${paragraphs(dialogue.pointRules.summary)}${paragraphs(dialogue.pointRules.basePoints)}${paragraphs(dialogue.pointRules.displayNotesWithMatching)}${paragraphs(dialogue.pointRules.matchingArcana)}<p>A missing reward is unknown, not zero. The listed decisions omit neutral prompts. Option numbers appear only when the source establishes their position.</p>${sources(dialogue.pointRules.sources)}${paragraphs(dialogue.relationshipRules.note)}${sources(dialogue.relationshipRules.sources)}</section>${dialogue.links.map(link => `<section id="${esc(link.id)}" data-lookup-item data-lookup-text="${esc(`${link.name} ${link.arcana}`)}"><h2>${esc(link.name)} · ${esc(link.arcana)} answers</h2><nav class="rank-links" aria-label="${esc(link.arcana)} ranks">${link.ranks.map(rank => `<a href="#${esc(link.id)}-${rank.rank}">Rank ${rank.rank}</a>`).join('')}</nav>${link.romance ? `<details><summary>Friendship and romance requirements</summary>${paragraphs(link.romance.notes)}${list(link.romance.flags?.map(flag => `Rank ${flag.rank}: ${flag.choiceCue}`))}<p>A romance-eligible conversation and a fully platonic conversation are different branches. Friendship after a confession is labeled separately.</p>${sources(link.sources)}</details>` : ''}${link.ranks.map(rank => `<details id="${esc(link.id)}-${rank.rank}"><summary>Rank ${rank.rank} answer cues</summary>${paragraphs(rank.note)}${rank.steps.map(step => `<div class="decision"><h3>${esc(step.cue || 'Response cue')}</h3>${step.route ? `<p class="route">Branch: ${esc(step.route)}</p>` : ''}${paragraphs(step.note)}${step.choices.map(choice => `<div class="choice"><p>${choice.index != null ? `<strong>Option ${choice.index}:</strong> ` : ''}${esc(choice.cue)}</p>${choice.route ? `<span class="route">${esc(choice.route)}</span>` : ''}${choice.basePoints != null ? `<span class="points">${choice.basePoints > 0 ? '+' : ''}${choice.basePoints} base affinity points</span>` : ''}${choice.displayNotesWithMatching != null ? `<span class="points">${choice.displayNotesWithMatching} displayed ${choice.displayNotesWithMatching === 1 ? 'note' : 'notes'} with matching Arcana</span>` : ''}${paragraphs(choice.effect)}</div>`).join('')}</div>`).join('')}${sources(rank.sources)}</details>`).join('')}${link.warnings?.length ? `<h3>Other conditions</h3>${list(link.warnings)}` : ''}${link.recovery ? `<details><summary>Recovery conditions</summary>${paragraphs(link.recovery.note)}${sources(link.recovery.sources)}</details>` : ''}${sources(link.sources)}<p class="back-index"><a href="#social-index">Back to character index</a></p></section>`).join('\n')}`;

const requestSlug = 'persona-3-reload-elizabeth-requests';
const requestBody = `<p class="notice">Completion means reported to Elizabeth. An item in your inventory does not count as a finished report. Request 101 contains late-game spoilers and is labeled without its title until opened.</p>${lookupForm({ guide: requestSlug, label: 'Find a request by number or title', placeholder: 'For example, 51 or muscle drink', noun: 'requests', options: [['deadline', 'With a reporting deadline']], categoryLabel: 'Deadline' })}<section id="request-index" data-lookup-intro><h2>Request number index</h2>${Array.from({ length: 6 }, (_, index) => {
  const entries = requests.entries.slice(index * 20, index * 20 + 20);
  return `<details><summary>${entries.length === 1 ? `Request ${entries[0].number}` : `Requests ${entries[0].number} to ${entries.at(-1).number}`}</summary><ul class="jump-list">${entries.map(entry => `<li><a href="#${entry.id}">#${entry.number} · ${esc(entry.spoiler ? 'Spoiler request' : entry.title)}</a></li>`).join('')}</ul></details>`;
}).join('')}</section><section data-lookup-intro><h2>Start dates, prerequisites and item opportunities</h2><p>The start date is the earliest request notice. Earlier reports, story progress, item access and Tartarus barriers can delay completion. A request can appear before its required floor opens.</p><p>Report-by dates are deadlines. Item opportunity dates identify a purchase or collection chance and are not reporting deadlines. If an opportunity has passed, an item already in your inventory can still satisfy a request with no fixed reporting deadline.</p></section><section><h2>All Elizabeth request solutions</h2>${requests.entries.map(entry => {
  const block = entry.floor ? tartarus.blocks.find(item => item.toFloor >= entry.floor) : null;
  return `<details id="${entry.id}" data-lookup-item data-lookup-number="${entry.number}" data-lookup-text="${esc(`${entry.number} ${entry.spoiler ? 'Spoiler request' : entry.title}`)}" data-lookup-category="${entry.deadline ? 'deadline' : 'none'}"><summary>#${entry.number} · ${esc(entry.spoiler ? 'Spoiler request: open to reveal' : entry.title)}${entry.deadline ? ` · Report by ${date(entry.deadline)}` : ''}</summary>${entry.spoiler ? `<h3>${esc(entry.title)}</h3>` : ''}<p>Earliest start: ${date(entry.start || entry.date)}. ${entry.deadline ? `Reporting deadline: ${date(entry.deadline)}.` : 'No fixed reporting deadline listed.'}</p>${entry.opportunity ? `<p class="notice">Item opportunity: ${date(entry.opportunity)}. This is an acquisition date, not a reporting deadline.</p>` : ''}${entry.prerequisites?.length ? `<p>Prerequisite reports: ${entry.prerequisites.map(number => `<a href="#request-${number}">#${number}</a>`).join(', ')}.</p>` : ''}${entry.floor ? `<p>Required floor: ${entry.floor}.${block ? ` This Tartarus section opens ${date(block.start)}.` : ''}</p>` : ''}${entry.requirements?.length ? `<h3>Requirements</h3>${list(entry.requirements)}` : ''}<h3>Solution</h3>${list(entry.steps, true)}${entry.persona ? `<p><a href="../persona-3-reload-fusion-guide/">Fusion recipes and unlock conditions</a> include ${esc(entry.persona)}${entry.skill ? ` and skill references for ${esc(entry.skill)}` : ''}.</p>` : ''}<h3>Rewards</h3>${list(entry.rewards)}${sources(entry.sources)}${block ? sources(block.sources) : ''}<p class="back-index"><a href="#request-index">Back to request index</a></p></details>`;
}).join('\n')}</section>`;

const basePersonas = availablePersonas();
if (basePersonas.length !== 173) throw new Error('Review fusion guide coverage after changing the base-game pool.');
const sourceFile = name => ({ url: `${provenance.repository}/blob/${provenance.commit}/src/app/p3r/data/${name}.json`, label: `${name} reference` });
const fusionSources = [sourceFile('demon-data'), sourceFile('fusion-chart'), sourceFile('demon-unlocks'), sourceFile('special-recipes')];
const examples = [
  ['Orpheus', 'Pixie', 'Angel'], ['Archangel', 'Silky', 'Oberon'], ['Oberon', 'Leanan Sidhe', 'Mithras'],
  ['Sati', 'Shiisaa', 'Mothman'], ['Shiki-Ouji', 'Kurama Tengu', 'Titania'], ['Titania', 'Thoth', 'Rangda'],
  ['Hecatoncheires', 'Shiki-Ouji', 'Siegfried'], ['Suzaku', 'Hecatoncheires', 'Daisoujou'], ['Koumokuten', 'Kurama Tengu', 'Raphael'],
];
for (const [first, second, result] of examples) if (fusePersonas(first, second)?.name !== result) throw new Error(`Fusion example changed: ${first} + ${second}`);
const personaLink = name => {
  const persona = basePersonas.find(entry => entry.name === name);
  if (!persona) throw new Error(`Missing base Persona: ${name}`);
  return `<a href="#persona-${persona.id}">${esc(name)}</a>`;
};
const unlockLabel = persona => persona.unlock ? persona.unlock.replace(/Max Link$/, 'Linked Episodes completed') : 'No extra unlock condition listed.';
const races = [...new Set(basePersonas.map(persona => persona.race))].sort();
const fusionSlug = 'persona-3-reload-fusion-guide';
const fusionBody = `<p class="notice">This page lists the ${basePersonas.length} base-game Personas. The calculator also supports DLC selections. Enabling DLC can change ordinary fusion results. These examples use no DLC Personas.</p><nav aria-label="Fusion guide contents"><a href="#fusion-examples">Recipe examples</a> · <a href="#special-recipes">Special fusions</a> · <a href="#persona-index">Persona list</a> · <a href="#fusion-unlocks">Unlock conditions</a></nav><section><h2>Fusion levels and recipe limits</h2><p>Ordinary fusion uses the ingredients' base levels and Arcana, not their raised levels in your save. A result's listed level does not include Social Link experience bonuses.</p><p>The examples establish the resulting Persona. They do not automatically give it a requested inherited skill. Native skills and their learning levels appear in each Persona entry. Inherited skills still depend on the ingredients and the result's inheritance rules.</p><p>The result can require a Social Link, request, Linked Episode or Tartarus unlock. The calculator shows that condition alongside the recipe. Check the Velvet Room's level and fusion requirements in your game.</p>${sources(fusionSources)}</section><section id="fusion-examples"><h2>Base-game fusion examples</h2><ul class="recipe-list">${examples.map(([first, second, result]) => `<li>${personaLink(first)} + ${personaLink(second)} → <strong>${personaLink(result)}</strong></li>`).join('')}</ul><p>These examples are checked against the tracker's fusion calculation whenever this page is generated. <a href="../../p3/#personas" data-umami-event="guide_open_tracker" data-umami-event-guide="${fusionSlug}">Use the calculator</a> for other ingredients or DLC settings.</p>${sources([sourceFile('demon-data'), sourceFile('fusion-chart')])}</section><section id="special-recipes"><h2>Special fusion recipes</h2><p>These fixed recipes use every listed ingredient. Their result can still require an unlock condition.</p>${basePersonas.filter(persona => persona.specialRecipe).map(persona => `<details id="special-${persona.id}"><summary>${esc(persona.name)} · level ${persona.lvl}</summary><p>${persona.specialRecipe.map(personaLink).join(' + ')} → ${personaLink(persona.name)}</p><p>Unlock: ${esc(unlockLabel(persona))}</p></details>`).join('')}${sources([sourceFile('special-recipes'), sourceFile('demon-unlocks')])}</section><section id="fusion-unlocks"><h2>Social Link, request and Linked Episode unlocks</h2><p>Each Persona below names its extra condition when the reference lists one. <strong>Linked Episodes completed</strong> refers to that character's episode chain, not ordinary dorm hangouts.</p><p><a href="../persona-3-reload-social-link-answers/">Social Link answers</a>, <a href="../persona-3-reload-elizabeth-requests/">request solutions</a> and <a href="../persona-3-reload-deadlines/">Linked Episode dates</a> cover those requirements.</p><ul class="jump-list">${basePersonas.filter(persona => persona.unlock).map(persona => `<li>${personaLink(persona.name)}: ${esc(unlockLabel(persona))}</li>`).join('')}</ul>${sources([sourceFile('demon-unlocks')])}</section><section id="persona-index"><h2>All ${basePersonas.length} base-game Personas by Arcana</h2>${lookupForm({ guide: fusionSlug, label: 'Find a Persona or native skill', placeholder: 'For example, Jack Frost or Matarukaja', noun: 'Personas', options: races.map(race => [race, race]), categoryLabel: 'Arcana' })}<nav aria-label="Persona Arcana">${races.map(race => `<a href="#arcana-${slugify(race)}">${esc(race)}</a>`).join(' · ')}</nav><p>Skills marked “initial” are present on the base Persona. Other numbers are the level at which that Persona learns the skill. These entries do not include arbitrary inherited skills.</p></section>${races.map(race => `<section id="arcana-${slugify(race)}" data-lookup-group><h2>${esc(race)} Personas</h2>${basePersonas.filter(persona => persona.race === race).sort((a, b) => a.lvl - b.lvl).map(persona => `<details id="persona-${persona.id}" data-lookup-item data-lookup-text="${esc(`${persona.name} ${persona.race} ${persona.lvl} ${Object.keys(persona.skills).join(' ')}`)}" data-lookup-category="${esc(persona.race)}"><summary>${esc(persona.name)} · level ${persona.lvl}</summary><p>Arcana: ${esc(persona.race)}. Base level: ${persona.lvl}.</p><p>Unlock: ${esc(unlockLabel(persona))}</p>${persona.specialRecipe ? `<p>Special recipe: ${persona.specialRecipe.map(personaLink).join(' + ')}.</p>` : '<p>Ordinary fusion. Available recipes depend on the enabled DLC pool.</p>'}<h3>Native skills</h3><ul>${Object.entries(persona.skills).map(([skill, level]) => `<li>${esc(skill)} · ${level < 1 ? 'initial' : `level ${level}`}</li>`).join('')}</ul>${persona.heart ? `<p>Heart item: ${esc(persona.heart)}${persona.heartlvl ? ` · level ${persona.heartlvl}` : ''}.</p>` : ''}${sources([sourceFile('demon-data'), ...(persona.unlock ? [sourceFile('demon-unlocks')] : [])])}</details>`).join('')}<p class="back-index"><a href="#persona-index">Back to Arcana index</a></p></section>`).join('')}<section><h2>Fusion reference credits</h2><p>Persona, recipe and unlock data comes from ${sourceLink({ url: SOURCE_URL, label: 'Persona 3 Reload fusion reference by aqiu384' })}, pinned to commit <code>${esc(provenance.commit)}</code> under the Unlicense. The calculator's normal and special fusion rules use the same reviewed reference.</p></section>`;

const pages = [
  { slug: socialSlug, title: 'Persona 3 Reload Social Link answers by rank', description: 'Find Persona 3 Reload Social Link answer cues for 19 characters and 190 ranks, with friendship and romance branches, affinity points, and sources.', intro: 'Answer cues for all 19 manual Social Links, covering ranks 1 through 10. Each rank lists relevant response choices, affinity rewards and branch conditions.', body: socialBody, tab: 'links', cta: 'Open Social Links and record your ranks' },
  { slug: requestSlug, title: 'Persona 3 Reload Elizabeth requests: all 101 solutions', description: 'All 101 Persona 3 Reload Elizabeth requests, with solutions, prerequisite reports, rewards, Tartarus floors, reporting deadlines and item opportunities.', intro: 'Look up an Elizabeth request by number, then check its solution, rewards and earlier reports. Reporting deadlines and item acquisition dates are labeled separately.', body: requestBody, tab: 'requests', cta: 'Open the request checklist' },
  { slug: fusionSlug, title: 'Persona 3 Reload fusion guide: recipes and Persona unlocks', description: 'Persona 3 Reload fusion recipes, special fusions, unlock conditions and native skills for 173 base-game Personas, with a working fusion calculator.', intro: 'Base-game fusion examples, fixed special recipes and the complete 173-Persona base-game list. Each entry includes its Arcana, base level, native skills and listed unlock condition.', body: fusionBody, tab: 'personas', cta: 'Open the Persona fusion calculator' },
];
for (const definition of pages) {
  const output = path.join(root, 'public/guides', definition.slug, 'index.html');
  const html = page(definition);
  if (process.argv.includes('--check')) {
    if (await readFile(output, 'utf8') !== html) throw new Error(`${definition.slug} is stale. Run node scripts/build-p3-playthrough-guides.mjs.`);
  } else {
    await mkdir(path.dirname(output), { recursive: true });
    await writeFile(output, html);
  }
}
console.log(`Reload playthrough guides ${process.argv.includes('--check') ? 'current' : 'generated'}: ${dialogue.links.reduce((sum, link) => sum + link.ranks.length, 0)} ranks, ${requests.entries.length} requests, ${basePersonas.length} base Personas.`);
